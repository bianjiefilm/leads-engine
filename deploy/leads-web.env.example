# leads-engine web (Next.js BFF) env sample (HUI-1748 L0)
# 目标路径(生产惯例):/etc/leads-engine/leads-web.env (0640, 不进 git)

# Go 服务地址(loopback;BFF 只打它,浏览器只打 BFF)
LEADS_SERVER_URL=http://127.0.0.1:18230
# 与 leads-server.env 中 LEADS_INTERNAL_TOKEN 同值
LEADS_INTERNAL_TOKEN=
# Next 监听端口
PORT=18330
